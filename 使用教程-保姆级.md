# PerfCollect-CN 保姆级使用教程

> ✅ **状态：现行（教程向）**。面向零基础同事的分步教程，与 `README.md` 互补：
> README 管"查"，本文管"跟着做"。指标口径问题请看 `指标说明.md`，换新手机请看 `devices.md`。
>
> 一个自研的安卓性能采集工具：测微信小游戏 / 安卓 App 的 **FPS（帧率）/ CPU / 内存 / 网络 / 温度**。
> 数据**全部保存在本地**，不传任何云端，自动生成 HTML 报告，双击就能看。
> 本文面向完全没接触过的同事，**每一步都教你点哪里、看到什么算成功**。

---

## 一、你要准备的东西

| 需要 | 说明 |
|---|---|
| 💻 Windows 电脑 | Win10 / Win11 都可以 |
| 📱 安卓手机 | 建议 Android 8.0 以上（工具是在 Android 14 上开发的） |
| 🔌 数据线 | 能传数据的线（别用只能充电的线） |

---

## 二、给电脑装 adb（一次性，约 5 分钟）

adb 是电脑和手机之间的"翻译官"，**必须装**。

1. **下载 platform-tools**（谷歌官方，含 adb）：
   - 打开网址：`https://developer.android.com/tools/releases/platform-tools`
   - 点下载 **Windows** 版 zip，解压到一个**好找的英文路径**，比如 `D:\platform-tools`（解压后里面有 `adb.exe`）

2. **把 adb 加进系统 PATH**（这样在任意地方都能用 adb 命令）：
   - 按 `Win` 键，输入"环境变量"，点 **"编辑系统环境变量"**
   - 点右下角 **"环境变量(N)…"**
   - 上半部分（用户变量）选中 **Path** → 点 **编辑** → 点 **新建** → 粘贴你的路径 `D:\platform-tools` → 确定 → 确定 → 确定

3. **验证是否装好**：
   - 按 `Win + R`，输入 `cmd` 回车
   - 输入 `adb version` 回车
   - ✅ 看到类似 `Android Debug Bridge version 1.0.41` 就是成功了
   - ❌ 提示"不是内部或外部命令"→ 检查第 2 步路径有没有写对，然后**关掉 cmd 重新打开**再试

---

## 三、手机开启"开发者选项 + USB 调试"（一次性，约 3 分钟）

1. 手机打开 **设置 → 关于手机**
2. **连续点击"版本号"7 次**（有的手机是"OS 版本"），直到提示"已进入开发者模式"
3. 回到设置，找到 **开发者选项**（一般在"系统"或"更多设置"里）
4. 打开 **USB 调试** 开关（弹窗确认就允许）
5. 用数据线连电脑，手机弹窗 **"是否允许 USB 调试"** → 勾选 **"始终允许"** → 允许

**验证连接**：cmd 里输入 `adb devices` 回车：
- ✅ 看到 `XXXXXXXX  device`（结尾是 **device** 而不是 unauthorized）→ 成功
- ❌ 看到 `unauthorized` → 手机重新拔插，勾选始终允许
- ❌ 什么都看不到 → 换一根数据线 / 换一个 USB 口 / 检查 USB 调试有没有开

---

## 四、获取工具（推荐方式 A）

> ⚠️ **本项目从 v72 起取消 exe / 安装包分发**，统一用 **zip 分发包**：
> 解压即用、内容可审计、不会被 SmartScreen 拦、没有安装与卸载残留。
> 你只需要装 uv（或 Python），见下面方式 A。
> （历史遗留的 `perfdog.exe` / 安装器如果你手上还有，可以继续用，但不再维护、也不再更新。）

### 方式 A：zip 分发包（推荐）

1. 找维护者拿最新分发包 **`perfcollect-cn-<版本>-<日期>.zip`**
2. **解压**到任意路径（**建议英文路径**，例如 `D:\perfcollect`；中文路径偶发 adb/路径问题）
3. 装一次 **uv**（自动准备 Python 运行时，最省事）：
   打开命令提示符输入 `pip install uv`；没有 pip 就去 <https://docs.astral.sh/uv/> 按说明装
   - 不想装 uv 也行：装 **Python 3.10+**，命令里用 `python` 代替 `uv run --no-project python`
4. 双击解压目录里的 **`start_perfcollect.bat`** —— 弹出命令行窗口并自动打开浏览器看板
5. 包内 **`先读我-使用说明.md`** 有 3 步上手与常见问题

> 首次双击会联网拉取一次依赖（几秒到几十秒），之后走本地缓存秒开。

### 方式 B：源码版（给会折腾的人 / 要改代码的人）

1. 打开仓库：`https://github.com/Darcy79/perfcollect-spark-darcy` → 绿色 **Code** → **Download ZIP** → 解压
   （或 `git clone https://github.com/Darcy79/perfcollect-spark-darcy.git`）
2. 电脑装 uv（推荐）或 Python 3.10+，双击运行 `start_perfcollect.bat`

### 维护者：怎么生成分发包

项目根目录双击 **`make_zip.bat`**（或执行 `python tools/make_zip.py`），
会在 `share/` 下生成 `perfcollect-cn-<版本>-<日期>.zip`。
版本号自动从 `CHANGELOG.md` 的状态头读取；包里**不含**采集数据（`collector/output/`）与内部文档。

---

## 五、开始采集（每次测都要做的 5 步）

1. **手机解锁**，打开 **微信 → 进入你要测的小游戏**，停在游戏画面
   - ⚠️ 让游戏**保持在最前台**，测的过程不要切后台、不要锁屏
2. **双击解压目录里的 `start_perfcollect.bat`**，会弹出命令行窗口
3. 看到这些输出就是正常在采了（数据目录打印的是**绝对路径**，数据就存在那里）：
   ```
   [+] 已连接设备: A3GD6R2A09011153
   [+] 目标进程: com.tencent.mm（匹配 appbrand） pid=1697
   [*] 开始采集（间隔 1.0s，Ctrl+C 停止）
   [*] 数据目录: output\20260821_144255

   ============================================================
     PerfCollect-CN 实时看板已启动
     实时看板  : http://localhost:8080          （即将自动打开浏览器）
     历史报告  : http://localhost:8080/report.html
     数据目录  : C:\Users\你\AppData\Local\Programs\PerfCollect-CN\output
     停止方式  : 本窗口按 Ctrl+C 一次停采集，再按一次退出
   ============================================================
   [14:42:55] FPS=59.85 Jank%=0.0 CPU总%=41.2 CPU进程%=98.3 PSS=487000kB 温度=38.0°C
   ```
4. 启动后约 1~2 秒浏览器**会自动打开实时看板**（`http://localhost:8080`）——六条曲线：FPS / 帧时间 / CPU / 内存 / 网络 / 温度，**实时刷新**
   - 没自动打开的话（个别环境默认浏览器没配好），自己开浏览器输 `http://localhost:8080`
5. 测完：回到命令行窗口，按 **`Ctrl + C` 两次** 结束（第一次停采集，第二次退出看板）

---

## 六、看报告（数据都在本地）

**数据存在哪**：都在**解压目录下的 `collector\output\`** 里——
- zip 分发版 / 源码版：`<你解压的位置>\collector\output\<时间戳>\`（例如 `D:\perfcollect\collector\output\`）
- 不确定的话，启动时命令行里那个"数据目录"打印的就是**绝对路径**，照着找即可

```
collector/output/
└── 20260821_144255/          ← 每次采集一个文件夹（按时间命名）
    ├── perfcollect_20260821_144255.jsonl    ← 原始数据（每行一个采样点）
    └── perfcollect_20260821_144255.html     ← ★ 自包含报告，双击就能看！
```

- **HTML 报告**：双击打开，带图表和数据统计（均值、P95、最大值等），**可以发给别人看**，不用装任何软件
- **历史报告**：看板开着的时候，浏览器访问 `http://localhost:8080/report.html` 可以翻历史
- **场景 Label**：采集开始后会自动处于 `Label1`。需要记录场景切换时，点击时间轨右侧紧凑的 `＋ 分段` 一次；系统会在当前最新采样时刻结束当前段并开始 `Label2/Label3…`，颜色自动轮换。点击任意 Label 色块都可改名，不必精准点小图标；输入期间色块长度仍随实时采样更新且不会丢失文字。历史看板的紧凑 Label 时间轨会随页面下翻吸附在全局拖动条下方，并与 Pin、图表绘图区共用同一坐标，随当前数据窗口裁剪和重排；鼠标停在图表上也能正常滚轮翻页。
- 换被测应用：看板左上角"被测应用"下拉框可以**热切换**，不用重启

---

## 七、常见问题（FAQ）

| 问题 | 解决办法 |
|---|---|
| 双击 bat 弹出"Windows 已保护你的电脑" | zip 分发版**不含 exe**，正常不会出现；若出现（例如文件从网盘另存后被系统标记），点 **"更多信息" → "仍要运行"** 即可，源码全公开可审 |
| 黑色窗口只有数据滚动，看不到图表 | 看板是**网页**，不在黑窗里——浏览器打开 `http://localhost:8080` 就是实时看板，`http://localhost:8080/report.html` 是历史报告页。正常情况启动后会自动打开浏览器 |
| 数据存哪了 | 解压目录下的 `collector\output\`（启动时"数据目录"一行打印的就是绝对路径） |
| `adb devices` 看不到手机 | 换数据线/换 USB 口；重新插拔；确认手机上"USB 调试"开着、弹窗选了"始终允许"；重启 adb（cmd 里 `adb kill-server` 再 `adb start-server`） |
| 提示 `未找到 com.tencent.mm 的进程` | 小游戏没在微信前台打开；先打开游戏再启动工具 |
| FPS 显示"无渲染层" | 游戏不在前台（回桌面/锁屏了）；把游戏切回前台等几秒自动恢复 |
| 双击 bat 没反应 / 闪一下就关 | 多半是没装 uv 或 Python（窗口里会有报错，见第四节）；首次运行要联网拉依赖，稍等几秒 |
| 端口 8080 被占用 | 用命令行启动并换端口：`python collector\main.py --web --port 9090`，浏览器访问 `http://localhost:9090` |
| 温度显示 `-` | 部分机型读不到电池温度/电流，属正常，其他指标不受影响 |
| CPU 进程百分比超过 100% | 正常！这是多核折算（一个进程可以占满多个核），不代表出错 |
| 想测某个 App（不是小游戏） | 看板"被测应用"下拉里选，或命令行：`python collector\main.py --package com.xxx.yyy --process-pattern ""` |
| 采集时手机变烫 | 正常——游戏满帧渲染 + 屏幕常亮 10 分钟以上都会升温，不代表工具异常 |

---

## 八、注意事项

- 🔒 **数据完全本地**：jsonl / HTML 都只存在你自己电脑上，**不上传任何服务器**
- 📊 **报告可分享**：HTML 报告是自包含文件，直接发给同事/领导就能看
- ⏱️ **采集别锁屏**：锁屏会导致 FPS 断采（游戏渲染层被系统销毁）
- 🔧 **源码全公开**：仓库里还有《架构设计》《指标说明》《代码评估》文档，想深入研究的同事可自行查看

---

*有任何问题，随时找工具的开发者。祝你测试顺利！🚀*
