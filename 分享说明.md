# 先读我 —— PerfCollect-CN 使用说明（分发版）

这是自研的**微信小游戏 / 安卓 App 性能采集工具**（FPS、CPU、内存、网络、温度）。
**所有数据只保存在你本机**，不上传云端。

---

## 一、3 步跑起来

1. **解压**到任意路径（建议英文路径，例如 `D:\perfcollect`，避免中文路径给 adb 带来的怪问题）。
2. **双击 `start_perfcollect.bat`** —— 启动采集 + 实时看板（浏览器会自动打开 `http://localhost:8080`）。
   - 只想看已有的历史报告、不连手机：双击 **`start_dashboard.bat`**。
3. 用数据线连上手机（**开启 USB 调试**），在网页里选择要采集的进程 → 开始采集。
   看板顶部有**启动向导**：它会只读探测候选进程（微信小游戏通常是 `com.tencent.mm:appbrandN`），
   推荐一个给你，**由你确认后**才开始记录。

---

## 二、环境要求

| 项 | 说明 |
|---|---|
| **uv**（推荐） | 会自动准备 Python 运行时，零安装负担。`pip install uv`，或见 <https://docs.astral.sh/uv/> |
| 或者 Python | 没装 uv 也能用：`python collector\main.py --web`（Python 3.10+） |
| **adb** | 采集需要。建议放在 `C:\platform-tools\`（采集器会自动优先用它），或加入 PATH |
| openpyxl | **只有"导出 XLSX"需要**；启动脚本已带 `--with openpyxl`，首次会联网自动获取 |

> 首次双击 `.bat` 时会联网拉取依赖，几秒到几十秒；之后走本地缓存，秒开。

---

## 三、数据在哪里

```
collector\output\<时间戳>\perfcollect_<时间戳>.jsonl    ← 原始采样数据（每行一个采样点）
collector\output\<时间戳>\perfcollect_<时间戳>.html     ← 自包含报告（双击即看，可直接分享）
```

报告页上的 📄 图标可以另存为**独立 HTML**，发给别人也能打开（无需本工具）。

---

## 四、常见问题

| 现象 | 处理 |
|---|---|
| 双击 bat 闪一下就没了 | 看窗口里的报错。多半是没装 uv/Python（按第二节装） |
| 报 `uv not found` | `pip install uv` 后重开窗口 |
| 采集时 FPS / 内存大段空白 | 报告顶部「数据完整度」卡片会写明原因：**链路读取失败**=USB/adb 抖动（换线换口、别同时开官方 PerfDog / Android Studio）；**无渲染层(不在前台)**=游戏没在前台 |
| 抓到的进程不对 | 用启动向导自己确认进程（微信会同时有 appbrand0/1/2 多个实例） |
| 端口 8080 被占用 | `python collector\main.py --web --port 9090` |
| 想测普通 App（不是小游戏） | 看板"被测应用"下拉里选，或 `--package com.xxx.yyy --process-pattern ""` |
| 杀毒软件拦截图/弹窗 | 本包是纯 Python + 静态网页，无 exe、无安装器，不会写系统目录；如有告警请核对文件来源 |

---

## 五、关于"没有 exe / 安装包"

本项目从 v72 起**取消 exe 与安装包分发**，统一用 zip：源码解压即用、内容可审计、
不会被 SmartScreen 拦、没有安装残留与卸载问题。你只需要 uv（或 Python）。

---

## 六、包内其它文档

| 文件 | 内容 |
|---|---|
| `使用教程-保姆级.md` | 从装 adb 到出报告的图文级步骤（第一次用建议读） |
| `指标说明.md` | 每个指标的含义、采集口径、可信度与常见误读（**看数据前建议看**） |
| `devices.md` | 已验证机型（荣耀 / OPPO）的适配差异与注意事项 |
| `README.md` | 项目全貌、目录结构、命令行参数、故障排查 |
