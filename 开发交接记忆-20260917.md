# PerfDog 开发交接记忆（2026-09-17）

> 用途：上下文压缩、任务转交或中断恢复时，先读本文件，再读 `CHANGELOG.md` 最新两条和
> `AGENTS.md`。本文件记录当前工作区状态，不代表已经提交到 Git。

## 当前边界

- 当前基线：工作区 v89，前端资源 v70；HEAD 为已提交并推送的 v88，v89 仍在工作区。
- 回归基线：Python 252 项、JS 149 项；按当前优先级未重打 v84–v89 zip，上次 zip 验证为 v83（51 文件、0.54 MB）。
- v87 已通过真实浏览器最小回归：实时图、Pin 蓝线/数值浮层、时间滑块、
  双报告切换/统计刷新、logcat 事件虚线均正常，控制台无警告或错误。
- 用户明确要求：暂不处理 SDK；不恢复任何 Windows EXE/安装包/PyInstaller 代码。
- 不修改 `collector/output/**` 和本机 `.app_labels.json`；保留历史 JSONL/HTML 兼容。

## 已完成但尚未提交

1. v75：`MetricMailbox` 保存 FPS 0.5 秒完整短窗，有界队列、丢弃计数、目标代次隔离。
2. v76：短窗 Jank 按 count/total 精确加权；P95/Max 使用有效短窗峰值；报告、Pin、导出兼容旧数据。
3. v77：`TargetContext` 统一 package/pattern/resolver/动态 pid/采集器/generation；修复 mismatch 使用旧 resolver。
4. v78：唯一 `JsonlWriter` 串行写入主 JSONL，停止时终止生产者并 drain/flush/close。
5. v79：schema v3 增加每指标 sampled_at_ms/age_ms/seq/is_reused，报告统计排除复用快照。
6. v80：`CaptureSession` 第一阶段统一停止/退出 Event、可中断等待和工作线程生命周期。
7. v81：修复切换事件顺序、worker 异常契约、采样完成时间语义和向导停止收口。
8. v82：真实 HTTP/SSE 覆盖全部 Web API 与静态入口；修复 SSE 断连噪声、
   Web socket/线程收口和备注文件句柄泄漏。
9. v83：新增只读一致性脚本与 CI 门禁，自动对比项目版本、实际 Python 测试数、
   五份状态文档测试计数与两页前端资源引用。
10. v84：`SamplerScheduler` 从 main 接管五类 worker 调度；`SampleAggregator` 接管
    mailbox 快照到 JSONL 行、新鲜度与 FPS 短窗摘要的组装。
11. v85：`RuntimeHealthTracker` 接管通道事件沿、断连连续计数/恢复沿和
    实时健全性状态；ADB/Web/落盘仍是显式副作用。
12. v86：`TargetMismatchTracker` 接管 AppBrand 错配去重/恢复，修复切换到非微信目标后旧告警残留；
    `format_sample_status` 接管控制台单行指标文本。
13. v87：`JsonlEventSink` 接管事件文件惰性/批量写入，logcat 按停产生者、排空、
    关 sink 的唯一顺序收尾；修复历史列表误显示 `*.events.jsonl`。
14. v87：新增双场景浏览器 smoke 服务，真实浏览器最小回归已通过。
15. 文档、CI、前端资源版本和测试计数已经随上述版本同步。

## 下一步执行顺序

1. 进入真机验收：先做 5–10 分钟短测，再做 30min+ 长测，重点核对目标进程/层、
   采样连续性、事件尾部、停止后 HTML 与资源回收。
2. 真机稳定后再评估 `report.html` 内联业务抽离与 `apk_label` 模糊测试，避免在验收前扩大改动面。
3. 按用户当前优先级，zip 解压后冒烟继续置后，功能与长测收敛后统一处理。

## JsonlWriter 已实现的边界

- `collector/main.py` 的主 JSONL 写入全部通过 `JsonlWriter.write()`，不得重新直接 `open(out_file)`。
- `events.jsonl` 是独立 logcat 文件，由 `JsonlEventSink` 管理；不属于主 JSONL writer，
  也不得在 `/api/runs` 里作为独立报告出现。
- writer 在注册 Web 回调前启动并先提交 meta；定时结束会先置 stop，再 drain/close。
- HTML 导出在 writer 关闭后执行；关闭后的迟到写入会抛出 `RuntimeError` 并由调用方收敛。

## 每轮交付前验证

```powershell
uv run --no-project python -m py_compile collector/main.py collector/*.py
uv run --no-project python -m unittest discover -s tests -p "test_*.py"
C:\Users\SparkGame\.cherrystudio\bin\bun.exe tests/test_nearest_cat.js
C:\Users\SparkGame\.cherrystudio\bin\bun.exe build web/assets/app.js --outfile <临时文件>
python tools/check_consistency.py
uv run --no-project python tools/make_zip.py --out <临时目录>
git diff --check
```

## 注意

- 不 commit、不 push。
- 改前端必须同时升级 `web/index.html` 与 `web/report.html` 各三处 `?v=`；纯采集端改动无需升级。
- 如果实际测试计数或版本变化，必须同步本文件、`AGENTS.md`、`CHANGELOG.md` 和优化待办。
