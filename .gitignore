# Python
__pycache__/
*.pyc
*.pyo

# 本机缓存（应用名解析缓存，每台设备不同）
collector/.app_labels.json
*.local.json

# 采集数据（测试记录：jsonl / 自包含 html / 事件标注）——如要上传测试数据，删除这一行即可
collector/output/

# 一次性验证产物（非代码）
验证统计卡片.html

# 分发 zip 产物（python tools/make_zip.py 的输出目录）
share/

# 其它
.uv-cache/
*.log

# 本地开发专用（内部文档/评估/交接/路线图等，只允许本地看，不入库）
docs-local/

# AGENTS.md：AI 协作者规约（需留在根目录才能被自动发现，但不公开）
AGENTS.md
